import 'package:flutter/material.dart';

// color palette for the app
class AppColors {
  static const primary = Color(0xFF52796F);       // used for app bars and major highlights
  static const accent = Color(0xFF84A98C);        // Used for buttons and accent UI
  static const background = Color(0xFFF0F5F1);    // Main background color
  static const card = Color(0xFFD9E4DD);          // card background
  static const text = Color(0xFF354F52);          // default text color
  static const error = Color(0xFFE63946);         // For error and delete actions
}
